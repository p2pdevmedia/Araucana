"use client";
import { useState } from "react";
const pins = [{ name: "San Martín de los Andes", detail: "Nuestra base en la Patagonia", left: "16%", top: "62%" }, { name: "Buenos Aires", detail: "Viajes grupales y privados", left: "23%", top: "68%" }, { name: "Santiago de Chile", detail: "Conexiones cordilleranas", left: "18%", top: "57%" }, { name: "Bariloche", detail: "Experiencias de invierno", left: "17%", top: "70%" }, { name: "Puerto Varas", detail: "Cruces y destinos del sur", left: "18%", top: "52%" }];
export function HistoricalMap() { const [scale, setScale] = useState(1); const [selected, setSelected] = useState(pins[0]); return <div className="historical-map"><div className="map-toolbar"><span>Destinos recorridos</span><div><button aria-label="Alejar mapa" onClick={() => setScale(Math.max(.85, scale - .15))}>−</button><button aria-label="Acercar mapa" onClick={() => setScale(Math.min(1.8, scale + .15))}>＋</button></div></div><div className="map-viewport"><div className="map-world" style={{ transform: `scale(${scale})` }}><div className="continent continent-one" /><div className="continent continent-two" /><div className="continent continent-three" />{pins.map((pin) => <button className={`map-pin ${selected.name === pin.name ? "is-selected" : ""}`} style={{ left: pin.left, top: pin.top }} key={pin.name} onClick={() => setSelected(pin)} aria-label={pin.name}>●</button>)}</div></div><div className="map-detail"><span className="map-detail-pin">●</span><div><strong>{selected.name}</strong><p>{selected.detail}</p></div></div></div>; }
